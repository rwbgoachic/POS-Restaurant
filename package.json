{
  "name": "@paysurity/api-middleware",
  "version": "1.0.0",
  "private": false,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo \"Building project...\" && exit 0"
  },
  "dependencies": {
    "@paysurity/common-utils": "latest",
    "express": "^4.18.2"
  }
}